# Agent notes

When product behavior changes, keep `VERSION`, README **Current version**, the latest `CHANGELOG.md` heading, and `docs/USER.md` / architecture in lockstep. The GitHub README stays a short purpose-and-capabilities page. Refresh GitHub About only when capabilities change, with no version number. See `.cursor/rules/docs-and-version.mdc`.


