# Agent notes

When product behavior changes, keep `VERSION`, README **Current version**, the latest `CHANGELOG.md` heading, stale user/architecture docs, and the GitHub About description (`gh repo edit --description`, must include the version) in lockstep. See `.cursor/rules/docs-and-version.mdc`.

