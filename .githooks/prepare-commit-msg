#!/bin/sh
root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
strip="$root/.githooks/strip-injected-coauthor"
[ -x "$strip" ] || strip="$(dirname "$0")/strip-injected-coauthor"
[ -x "$strip" ] && exec "$strip" "$1"
exit 0
